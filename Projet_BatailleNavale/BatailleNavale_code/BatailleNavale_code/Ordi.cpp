#include <string>
#include "Ordi.h"
#include <random>
using namespace std;

void Ordi::joue() {
	if (etat == "cherche") {
		int x = rand() % 5 + 5 * cadranx;
		int y = rand() % 5 + 5 * cadrany;
	}
	if (etat == "calibrage") {
		if 

	}
	if (etat == "extermination") {

	}

}
